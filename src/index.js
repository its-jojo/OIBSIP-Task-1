import React from 'react';
import ReactDOM from 'react-dom/client';
import AppleLandingPage from './AppleLandingPage';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<AppleLandingPage />);
